alias irb='irb --readline -r irb/completion'

if [[ -s /Users/ramiel/.rvm/scripts/rvm ]] ; then 
  source /Users/ramiel/.rvm/scripts/rvm ; 
fi

if [[ -s /usr/local/rvm/scripts/rvm ]] ; then 
  source /usr/local/rvm/scripts/rvm ; 
fi
